[pytest]
testpaths = tests
pythonpath = .
addopts = -ra
filterwarnings =
    # an all-off AugConfig builds a Compose with no transforms; that is expected
    ignore:Got processor for:UserWarning:albumentations
